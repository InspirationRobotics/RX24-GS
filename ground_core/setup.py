from setuptools import setup, find_packages

setup(
    name='ground_core',
    version='0.1',
    description='Core Ground Station package for Inspiration RobotX',
    author='Eesh Vij',
    packages=find_packages(include=['ground_core'])
)